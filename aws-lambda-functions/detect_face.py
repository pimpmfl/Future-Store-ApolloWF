import json

def lambda_handler(event, context):
    # TODO get base64 encoded image string and run it through Amazon Rekognition
    # split the image into multiple ones with faces
    return {
        'base64Image': 'someStringDetectFaces'
    }
